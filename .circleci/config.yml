version: 2
jobs:
  build:
    docker:
      - image: circleci/python:3.7.3
    steps:
      - checkout
      - run:
          name: Install Python deps 
          command: |
            sudo pip install -r requirements.txt 
            sudo pip install flask_project/backend/core_package/
            python flask_project/backend/service/run.py
